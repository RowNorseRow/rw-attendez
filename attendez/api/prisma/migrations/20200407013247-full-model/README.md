# Migration `20200407013247-full-model`

This migration has been generated by Darren Tebo at 4/7/2020, 1:32:47 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."Attendance" (
    "date" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "dayofweek" TEXT NOT NULL DEFAULT '' ,
    "fifthday" INTEGER NOT NULL DEFAULT 0 ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "present" INTEGER NOT NULL DEFAULT 0 ,
    "student" INTEGER NOT NULL  ,FOREIGN KEY ("student") REFERENCES "Student"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."AttendanceHistory" (
    "attendance" INTEGER NOT NULL  ,
    "expirationDate" DATE  DEFAULT '1970-01-01 00:00:00' ,
    "expired" INTEGER NOT NULL DEFAULT 0 ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,FOREIGN KEY ("attendance") REFERENCES "Attendance"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
    "email" TEXT NOT NULL DEFAULT '' ,
    "firstName" TEXT   ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "lastName" TEXT   ,
    "type" TEXT   
) 

INSERT INTO "quaint"."new_User" ("email", "firstName", "id", "lastName", "type") SELECT "email", "firstName", "id", "lastName", "type" FROM "quaint"."User"

DROP TABLE "quaint"."User";

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Student" (
    "email" TEXT NOT NULL DEFAULT '' ,
    "firstName" TEXT   ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "lastName" TEXT   ,
    "user" INTEGER NOT NULL  ,FOREIGN KEY ("user") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Student" ("email", "firstName", "id", "lastName", "user") SELECT "email", "firstName", "id", "lastName", "user" FROM "quaint"."Student"

DROP TABLE "quaint"."Student";

ALTER TABLE "quaint"."new_Student" RENAME TO "Student";

CREATE UNIQUE INDEX "quaint"."Student.email" ON "Student"("email")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200407012327-relational-users..20200407013247-full-model
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider      = "prisma-client-js"
@@ -16,9 +16,8 @@
   email String  @unique
   firstName String?
   lastName  String?
   type      String?
-  studentID Int
   student   Student[]
 }
 model Student {
@@ -26,6 +25,21 @@
   email   String  @unique
   firstName String?
   lastName  String?
   user    User
-  userId  Int
+}
+
+model Attendance {
+  id  Int   @id @default(autoincrement())
+  student Student
+  date    DateTime @default(now())
+  dayofweek   String
+  present   Int @default(0)
+  fifthday  Int @default(0)
+}
+
+model AttendanceHistory {
+  id  Int  @id @default(autoincrement())
+  attendance Attendance
+  expired   Int @default(0)
+  expirationDate  DateTime?  @default(now())
 }
```


